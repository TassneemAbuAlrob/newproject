import 'package:flutter/material.dart';
import 'package:finalfrontproject/screens/components/body.dart';

class welcomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
